# Memory bank inventory fixture

Minimal consumer repo for Assert-MemoryBankInventory.ps1 smoke coverage.
Assert copies this seed to a temp work root; do not commit generated `memory-bank/.inventory/`.
