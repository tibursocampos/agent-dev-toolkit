#Requires -Version 5.1
# Grok Build SessionStart hook - exit 0; no trust UI interaction.
# Hooks trust via Grok /hooks-trust or --trust is a human operational step; smoke validates files only.
exit 0
