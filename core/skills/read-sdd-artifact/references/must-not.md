## Must not

- Return a partial `source_context` on fail (REQ-005 / RNF-001 / TE01)
- Accept `..`, OS absolute paths, or paths outside `features/` (SEC path-guard)
- Treat CONTINUITY / ANALYSIS / ARCH / SEC / CHANGE / memory-bank as supported kinds in this skill
- Dump full feature trees or `memory-bank/` (`SELECTIVE-RETRIEVAL` / `SR-NO-FULL-DUMP`)
- Invent skills, Write production code, commit, or push
- Preload all `references/*.md` or compress reject reasons via Caveman
