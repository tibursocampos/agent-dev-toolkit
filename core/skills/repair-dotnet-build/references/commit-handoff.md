## Commit handoff

After local validation:

```
/commit
```

The commit skill enforces `feature/<slug>` / `feat/<id>` and Conventional Commits. repair-dotnet-build does not call tracker APIs or link work items.

---
