## Context management

Per `{{TOOLKIT_ROOT}}/rules/context-management.mdc`: after detection + requirements + large scaffold, checkpoint at ≥ 40% context; hand off with paths and pending checklist items.
