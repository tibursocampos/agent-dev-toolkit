## Must not

- Extract string keys with generic names (like `Text1`, `String2`)
- Mutate logger strings or database connection configurations
- Write code or start another workflow before the user chooses Option A / B / D
- Preload unrelated stack guideline packs or dump `memory-bank`/full PRD bodies
- Invent localization frameworks not present in the repository
